'use strict';

const expect = require('chai').expect;
const GRPX = require('../dist/index.js');

describe('GRPX lib function test', () => {
  it('pack', () => {
    expect(true).to.equal(true);
  });
  it('unpack', () => {
    expect(true).to.equal(true);
  });
});